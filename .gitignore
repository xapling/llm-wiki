.venv/
.chroma/
.DS_Store
.idea/
__pycache__/
*.pyc
.env
.env.local
# Laufzeit-Artefakte (aufgezeichnete Antworten, hochgeladene Demo-Dokumente)
demo/cache/
demo/eingang/upload-*
corpus/upload-*
# Hinweis: slides/praesentation.html wird generiert (make slides), liegt aber
# bewusst mit im Repo, damit die Folien ohne Setup anschaubar sind.
# Arbeitskopie — wird von `make reset` aus seed/wiki erzeugt (nur die Wurzel!)
/wiki/
